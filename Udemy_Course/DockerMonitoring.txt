#Check all the process running on Container
docker container top ContainerID/ContainerName

#Check all details about the Container
docker container inspect ContainerID/ContainerName

#Check all  live data stream for running containers.
docker container stats